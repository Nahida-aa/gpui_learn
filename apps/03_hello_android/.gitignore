# gen/android/ 由 `gpui-cli android init` 生成，不提交（重新生成即可）。
gen/

# 由 cargo ndk 编译生成的原生库（在 gen/android/app/src/main/jniLibs/），不提交。
gen/android/app/src/main/jniLibs/

# Gradle 构建产物
gen/android/app/build/
gen/android/.gradle/
.gradle/
build/
