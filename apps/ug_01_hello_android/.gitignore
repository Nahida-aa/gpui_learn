# gen/android/ 由 `gpui-cli android init` 生成。生成的 Gradle 源码（build.gradle.kts、
# AndroidManifest.xml、settings.gradle.kts、gradlew、wrapper、res 等）纳入版本追踪
# （对齐 Tauri）；构建产物与拷贝资源由 gen/android/.gitignore 忽略。

# Gradle 构建产物（仓库根级，兜底）
.gradle/
build/
